# Environment / secrets
.env
.env.*
!.env.example
.envrc

# Python byte-compiled / optimized / DLL files
__pycache__/
*.py[codz]
*$py.class
*.so

# Distribution / packaging
.Python
build/
develop-eggs/
dist/
downloads/
eggs/
.eggs/
lib/
lib64/
parts/
sdist/
var/
wheels/
share/python-wheels/
*.egg-info/
.installed.cfg
*.egg
MANIFEST

# PyInstaller
*.manifest
*.spec

# Installer logs
pip-log.txt
pip-delete-this-directory.txt

# Unit test / coverage reports
htmlcov/
.tox/
.nox/
.coverage
.coverage.*
.cache
nosetests.xml
coverage.xml
*.cover
*.py.cover
.hypothesis/
.pytest_cache/
cover/

# Jupyter / IPython
.ipynb_checkpoints
profile_default/
ipython_config.py

# Virtual environments
venv/
env/
ENV/
.venv/
env.bak/
venv.bak/

# Type checkers / linters
.mypy_cache/
.dmypy.json
dmypy.json
.pyre/
.pytype/
.ruff_cache/

# IDE
.idea/
.vscode/
*.swp
*.swo

# OS
.DS_Store
Thumbs.db

# Project — traces and logs
# Ignore everything in traces/ (raw traces may contain PII from personal
# trees) EXCEPT the redacted/ subdir, which holds pseudonymized traces
# safe to commit as demonstration artifacts. The .map.json mapping files
# MUST stay local — they re-identify real people.
traces/*
# Pseudonymized traces from personal trees — safe to commit
!traces/redacted/
traces/redacted/*.map.json
# Public-data demo traces (Kennedy, Habsburg, Queen) — safe to commit as-is
!traces/demos/
*.log

# Deprecated migration-map PoC
# Preserved on the feature/migration-map branch on origin; not part of
# the main project scope.
app/
data/geodata/

# ---------------------------------------------------------------
# PII — personal genealogical data. NEVER commit.
# ---------------------------------------------------------------
# Personal GEDCOM files
data/PII Trees/
# DNA match exports (MyHeritage, GEDmatch, AncestryDNA, etc.)
data/DNA/
data/dna/
# eval/results/ contains a mix of public-data and PII-bearing JSON.
# Default to ignored; allow-list public-data result patterns; explicitly
# block any *moore* file regardless of pattern.
eval/results/*
!eval/results/*.md
!eval/results/eval_*.json
!eval/results/isolation_*.json
!eval/results/cross_vendor_2026042[6-8]*.json
!eval/results/cross_vendor_hypo_*_jfk.json
!eval/results/cross_vendor_hypo_*_tier3*.json
!eval/results/multi_critic_*_jfk.json
!eval/results/multi_critic_*_tier3*.json
!eval/results/ablation_*.json
eval/results/*moore*
eval/results/*Moore*
